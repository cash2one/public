# Example YAML to get you started quickly.
# Be aware that YAML has indentation based scoping.
# Code completion support is available so start typing for available options.
swagger: '2.0'

# This is your document metadata
info:
  version: "0.0.1"
  title: <极管家接口>

host: yaoshi.sdo.com

schemes:
  - http

# Describe your paths here
paths:
  # This is a path endpoint. Change it.
  /apipool:
    # This is a HTTP operation
    get:
      # Describe this verb here. Note: you can use markdown
      description: |
        txz_as_mixed.dataCollection.saveAppInstallData
        `无映射`
      # This is array of GET operation parameters:
      parameters:
        # An example parameter that is in query and is required
        -
          name: dataTimeMs
          in: query
          description: 数据对应的时间戳
          required: true
          type: number
          format: long
        -
          name: phone
          in: query
          description: 手机号码
          required: true
          type: string
        -
          name: version
          in: query
          description: 版本
          required: true
          type: string
        -
          name: type
          in: query
          description: 数据类型
          required: true
          type: string
        -
          name: content
          in: query
          description: 数据类型
          required: true
          type: string

      # Expected responses for this operation:
      responses:
        # Response code
        '200':
          description: ok
